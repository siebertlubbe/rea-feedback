<% @feedbacks.each do |feedback| %>
   <div class="responseWrap">
     <div class="responseHeader">
       <span class="sendToSupport"><input disabled="disabled" class="pushToSupport" type="submit" value="sent to support" /></span>
       <span class="responseUrl"><%= feedback.url %></span>
       <span class="responseScore <%= feedback.emotion %>" title="<%= feedback.emotion %>">
         <%= feedback.emotion.upcase %>
       </span>
       <span class="responseTrend <%= feedback.trend %>" title="treding <%= feedback.trend %>"><%= feedback.trend %></span>
       <h2 class="responseProduct"><%= feedback.url.gsub(/http:\/\//, '').split('/').first %></h2>
       <div class="clearAll">&nbsp;</div>
     </div>
     <div class="responseText">
       <%= feedback.comment %>
       <span class="responseTime"><%= feedback.created_at.strftime('%Y-%m-%d %H:%M:%S') %></span>
     </div>
     
   </div>
<% end %>

<script type="text/javascript">
  var numberOfFeedbacks = <%= @feedbacks.size %>
  
  var t = setTimeout("getFeedbacks(filter)", 3500);

</script>